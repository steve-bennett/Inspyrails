<%= form_for(@userdream) do |f| %>
  <% if @userdream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userdream.errors.count, "error") %> prohibited this userdream from being saved:</h2>

      <ul>
      <% @userdream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :dream %><br />
    <%= f.text_field :dream %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
