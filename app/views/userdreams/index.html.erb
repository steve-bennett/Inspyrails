<h1>Listing userdreams</h1>

<table>
  <tr>
    <th>User</th>
    <th>Dream</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @userdreams.each do |userdream| %>
  <tr>
    <td><%= userdream.user %></td>
    <td><%= userdream.dream %></td>
    <td><%= link_to 'Show', userdream %></td>
    <td><%= link_to 'Edit', edit_userdream_path(userdream) %></td>
    <td><%= link_to 'Destroy', userdream, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Userdream', new_userdream_path %>
