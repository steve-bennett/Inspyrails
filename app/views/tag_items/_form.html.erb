<%= form_for(@tag_item) do |f| %>
  <% if @tag_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_item.errors.count, "error") %> prohibited this tag_item from being saved:</h2>

      <ul>
      <% @tag_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="field">
    <%= f.label :item %><br />
    <%= f.text_field :item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
