<%= form_for(@tag_dream) do |f| %>
  <% if @tag_dream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_dream.errors.count, "error") %> prohibited this tag_dream from being saved:</h2>

      <ul>
      <% @tag_dream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="field">
    <%= f.label :dream %><br />
    <%= f.text_field :dream %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
