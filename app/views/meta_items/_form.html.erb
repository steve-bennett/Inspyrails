<%= form_for(@meta_item) do |f| %>
  <% if @meta_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meta_item.errors.count, "error") %> prohibited this meta_item from being saved:</h2>

      <ul>
      <% @meta_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meta_key %><br />
    <%= f.text_field :meta_key %>
  </div>
  <div class="field">
    <%= f.label :meta_value %><br />
    <%= f.text_field :meta_value %>
  </div>
  <div class="field">
    <%= f.label :item %><br />
    <%= f.text_field :item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
