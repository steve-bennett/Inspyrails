<h1>Listing tag_userdreams</h1>

<table>
  <tr>
    <th>Tag</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tag_userdreams.each do |tag_userdream| %>
  <tr>
    <td><%= tag_userdream.tag %></td>
    <td><%= tag_userdream.user %></td>
    <td><%= link_to 'Show', tag_userdream %></td>
    <td><%= link_to 'Edit', edit_tag_userdream_path(tag_userdream) %></td>
    <td><%= link_to 'Destroy', tag_userdream, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tag userdream', new_tag_userdream_path %>
