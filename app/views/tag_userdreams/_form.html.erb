<%= form_for(@tag_userdream) do |f| %>
  <% if @tag_userdream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_userdream.errors.count, "error") %> prohibited this tag_userdream from being saved:</h2>

      <ul>
      <% @tag_userdream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
